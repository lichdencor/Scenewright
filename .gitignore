# CMake / build output
build/
install/
out/

# vcpkg (installed packages + the tool itself, if cloned in-tree)
vcpkg_installed/
vcpkg/

# IDE
.vs/
.idea/
*.user

# CMake compile commands (regenerated per-build)
compile_commands.json

# Personal machine/setup notes — never committed, see docs/build-and-test-guide.md
# for the generic public version
*.local.md

# Planning/research docs (ADRs, roadmap, investigation notes, etc.) — personal
# working notes, not part of the public repo.
docs/
